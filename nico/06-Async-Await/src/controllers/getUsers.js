let data = require('../utils/data')

// esta funcion se encarga de obtener y retornar la info de todos los usuarios
const getUsers = async () => {
    // throw Error("Hubo un error")
    return data;
};

module.exports = getUsers;